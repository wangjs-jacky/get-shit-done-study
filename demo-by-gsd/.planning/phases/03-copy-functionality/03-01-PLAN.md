---
phase: 03-copy-functionality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/ToastContext.tsx
  - src/components/Toast.tsx
  - src/context/__tests__/ToastContext.test.tsx
  - src/components/__tests__/Toast.test.tsx
autonomous: true
requirements: [COPY-02]
user_setup: []

must_haves:
  truths:
    - "Toast component displays success message when triggered"
    - "Toast auto-dismisses after 3 seconds"
    - "Toast renders via Portal to avoid z-index conflicts"
    - "Toast animation is smooth (fade in/out)"
  artifacts:
    - path: "src/context/ToastContext.tsx"
      provides: "Global toast state management via Context"
      exports: ["ToastProvider", "useToast"]
    - path: "src/components/Toast.tsx"
      provides: "Visual toast notification component"
      contains: "createPortal"
  key_links:
    - from: "src/context/ToastContext.tsx"
      to: "src/components/Toast.tsx"
      via: "conditional rendering in provider"
      pattern: "toast && <Toast"
---

<objective>
创建 Toast 通知系统（Context + Portal 模式），为后续 CopyButton 提供反馈机制。

Purpose: 提供专业的 toast 通知系统，替代现有的 alert() 调用，提升用户体验。
Output: ToastContext（状态管理）+ Toast 组件（Portal 渲染）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-copy-functionality/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from codebase -->

From src/types/style.ts:
```typescript
export interface Style {
  id: string;
  name: string;
  description: string;
  cssVariables: Record<string, string>;
  promptText: string;
}
```

From vitest.config.ts:
```typescript
// Test framework: Vitest 3.x with jsdom
// Setup file: ./src/test/setup.ts
// Include: src/**/*.test.ts, src/**/*.test.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ToastContext with Provider pattern</name>
  <files>src/context/ToastContext.tsx, src/context/__tests__/ToastContext.test.tsx</files>
  <behavior>
    - Test 1: ToastProvider renders children correctly
    - Test 2: useToast throws error when used outside ToastProvider
    - Test 3: showToast updates toast state with message and type
    - Test 4: Toast auto-dismisses after 3 seconds (use fake timers)
  </behavior>
  <action>
    Create ToastContext.tsx in src/context/ with:
    1. ToastContextValue interface with showToast(message: string, type: 'success' | 'error'): void
    2. ToastProvider component wrapping children with Context.Provider
    3. Internal toast state: { message: string; type: string } | null
    4. showToast function: sets toast state, then setTimeout for 3s auto-dismiss
    5. useToast hook: useContext with null check (throw error if outside provider)
    6. Conditional Toast rendering in provider (toast && <Toast .../>)

    Reference RESEARCH.md Pattern 2 for implementation structure.
    Use React 19.x createContext and useCallback patterns.
    Ensure cleanup of timeout on unmount or when new toast appears.
  </action>
  <verify>
    <automated>npm test -- --run ToastContext</automated>
  </verify>
  <done>ToastContext exports ToastProvider and useToast, all 4 tests pass, timeout cleanup implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Toast component with Portal</name>
  <files>src/components/Toast.tsx, src/components/__tests__/Toast.test.tsx</files>
  <behavior>
    - Test 1: Toast renders message text correctly
    - Test 2: Toast applies success styles when type='success'
    - Test 3: Toast applies error styles when type='error'
    - Test 4: Toast has fade-in animation on mount
    - Test 5: Toast has role="alert" and aria-live="polite" for accessibility
  </behavior>
  <action>
    Create Toast.tsx in src/components/ with:
    1. Props: { message: string; type: 'success' | 'error' }
    2. Use createPortal from react-dom to render to document.body
    3. Fixed position: bottom-right (bottom-4 right-4)
    4. Success styling: bg-[var(--color-primary)] text-[var(--color-bg)]
    5. Error styling: bg-[var(--color-red)] text-white
    6. Animation: transition-all duration-300 with opacity/translate-y
    7. Internal isVisible state triggered via useEffect + requestAnimationFrame
    8. Accessibility: role="alert" aria-live="polite"

    Reference RESEARCH.md Pattern 3 for implementation structure.
    Use Tailwind classes for styling, CSS variables for colors.
  </action>
  <verify>
    <automated>npm test -- --run Toast.test</automated>
  </verify>
  <done>Toast renders via Portal with success/error variants, animation works, accessibility attributes present, all 5 tests pass</done>
</task>

</tasks>

<verification>
- [ ] ToastContext.tsx exists with ToastProvider and useToast exports
- [ ] Toast.tsx exists with createPortal usage
- [ ] All tests pass: npm test -- --run ToastContext Toast.test
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
1. User can trigger toast via useToast().showToast('message', 'success')
2. Toast appears in bottom-right corner with smooth animation
3. Toast auto-dismisses after 3 seconds
4. Toast renders outside component hierarchy (Portal verified in tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-copy-functionality/03-01-SUMMARY.md`
</output>
