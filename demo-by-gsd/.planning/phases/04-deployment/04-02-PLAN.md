---
phase: 04-deployment
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [DEPL-01, DEPL-02]

must_haves:
  truths:
    - "Site is accessible at https://wangjs-jacky.github.io/frontend-design-gallery"
    - "All features work on deployed site (preview, style switch, copy)"
    - "Pushing changes triggers automatic redeployment"
  artifacts:
    - path: "GitHub Pages deployment"
      provides: "Live site at public URL"
      verify: "curl returns 200"
  key_links:
    - from: "workflow completion"
      to: "live site"
      via: "GitHub Pages deployment"
      pattern: "site URL accessible"
---

<objective>
Verify deployment to GitHub Pages is working and CI/CD pipeline functions correctly.

Purpose: Confirm the site is live and automatic deployment works end-to-end.
Output: Verified deployment at https://wangjs-jacky.github.io/frontend-design-gallery
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment/04-RESEARCH.md

## Deployment URL

https://wangjs-jacky.github.io/frontend-design-gallery

## Prerequisites (from Plan 04-01)

- `.github/workflows/deploy.yml` created
- astro.config.mjs has correct `site` and `base` settings

## User Setup Required

Before this plan can complete, user must:
1. Go to Repository Settings > Pages
2. Set "Build and deployment" > "Source" to "GitHub Actions"
3. Push the workflow file to main branch
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push workflow file</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    Stage and commit the workflow file created in Plan 04-01:

    ```bash
    git add .github/workflows/deploy.yml
    git commit -m "ci: add GitHub Actions workflow for GitHub Pages deployment"
    ```

    Then push to trigger the workflow:

    ```bash
    git push origin main
    ```

    **Note:** The push will trigger the GitHub Actions workflow automatically.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "GitHub Actions workflow" && echo "Commit created"</automated>
  </verify>
  <done>
    - Workflow file committed to repository
    - Push to main branch completed
    - GitHub Actions workflow triggered
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GitHub Actions workflow for automatic deployment to GitHub Pages</what-built>
  <how-to-verify>
    ## Step 1: Configure GitHub Pages Source

    1. Go to your repository on GitHub: https://github.com/wangjs-jacky/frontend-design-gallery
    2. Navigate to **Settings** > **Pages**
    3. Under "Build and deployment" > "Source", select **"GitHub Actions"**
    4. Save the setting

    ## Step 2: Verify Workflow Execution

    1. Go to **Actions** tab in your repository
    2. You should see a workflow run "Deploy to GitHub Pages"
    3. Wait for both `build` and `deploy` jobs to complete (green checkmarks)
    4. Expected duration: ~2-3 minutes

    ## Step 3: Verify Site Accessibility

    1. Visit: https://wangjs-jacky.github.io/frontend-design-gallery
    2. Verify the page loads without 404 errors
    3. Verify CSS and JavaScript load correctly (no console errors)
    4. Verify the layout shows: left preview pane + right style list

    ## Step 4: Verify All Features Work

    1. Click on a style card - preview should update instantly
    2. Start the Pomodoro timer - timer should countdown
    3. Click "Copy Prompt" button - toast notification should appear
    4. Test on mobile viewport (responsive layout)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify CI/CD auto-trigger (DEPL-02)</name>
  <files>src/pages/index.astro</files>
  <action>
    Make a small, visible change to verify automatic deployment works:

    1. Read the current index.astro file
    2. Add a comment or small text change that will be visible
    3. Commit with message: "test: verify auto-deploy CI/CD"
    4. Push to main

    This verifies that pushing to main automatically triggers a new deployment.

    **Suggested change:** Add a HTML comment at the top of the page:
    ```astro
    ---
    <!-- Last deployment test: 2026-03-16 -->
    ---
    ```

    Or simply update the page title slightly to confirm deployment worked.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "auto-deploy" && echo "Test commit pushed"</automated>
  </verify>
  <done>
    - Small change committed and pushed
    - New workflow run triggered automatically
    - Confirms DEPL-02 (auto-trigger on push) works
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Auto-deployment trigger verification</what-built>
  <how-to-verify>
    ## Verify Auto-Deploy Worked

    1. Go to **Actions** tab
    2. Confirm a new workflow run was triggered by the "test: verify auto-deploy" commit
    3. Wait for deployment to complete
    4. Visit the site and confirm the change is visible
    5. Refresh the page (Ctrl+Shift+R to bypass cache) to see the update

    This confirms the full CI/CD pipeline:
    - Push to main triggers workflow (DEPL-02)
    - Workflow builds and deploys automatically (DEPL-01)
    - Changes are live on GitHub Pages
  </how-to-verify>
  <resume-signal>Type "approved" if auto-deploy works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Final verification checklist:
- [ ] Site accessible at https://wangjs-jacky.github.io/frontend-design-gallery
- [ ] All features work (preview, style switch, timer, copy)
- [ ] Pushing to main triggers automatic deployment
- [ ] No manual intervention required for deployment
</verification>

<success_criteria>
1. Site loads successfully at the GitHub Pages URL
2. All UI features work on deployed site
3. CI/CD pipeline triggers automatically on push to main
4. Deployment completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-02-SUMMARY.md`

Also update REQUIREMENTS.md to mark DEPL-01, DEPL-02, DEPL-03 as complete.
</output>
