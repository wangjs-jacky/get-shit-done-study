---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs, tsconfig.json, src/styles/global.css, vitest.config.ts]
autonomous: true
requirements: [LAYT-03]
user_setup: []

must_haves:
  truths:
    - "pnpm dev starts development server without errors"
    - "pnpm build produces static output in dist/ directory"
    - "Tailwind CSS classes apply correctly to elements"
    - "React components can be imported and used"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: '"astro", "react", "tailwindcss", "@tailwindcss/vite"'
    - path: "astro.config.mjs"
      provides: "Astro configuration with React and Tailwind"
      exports: ["defineConfig"]
      contains: "site: 'https://wangjs-jacky.github.io'"
    - path: "tsconfig.json"
      provides: "TypeScript configuration for React JSX"
      contains: '"jsx": "react-jsx"'
    - path: "src/styles/global.css"
      provides: "Tailwind CSS import and custom properties"
      contains: "@import \"tailwindcss\""
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      contains: "defineConfig"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/styles/global.css"
      to: "Tailwind CSS"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
---

<objective>
Initialize Astro 5.x project with React 19.x and Tailwind 4.x using the Vite plugin approach.

Purpose: Establish the project foundation with correct configuration for GitHub Pages deployment and enable React islands architecture.
Output: Working Astro project with Tailwind CSS and React integration, ready for component development.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key patterns from research that executors must follow -->

From 01-RESEARCH.md - astro.config.mjs pattern:
```javascript
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://wangjs-jacky.github.io',
  base: '/frontend-design-gallery',
  output: 'static',
  integrations: [react()],
  vite: {
    plugins: [tailwindcss()],
  },
});
```

From 01-RESEARCH.md - tsconfig.json pattern:
```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react"
  }
}
```

From 01-RESEARCH.md - global.css pattern:
```css
@import "tailwindcss";

:root {
  --color-bg: #0a0a0b;
  --color-primary: #00ff88;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Astro project with pnpm</name>
  <files>package.json, astro.config.mjs, tsconfig.json</files>
  <action>
    1. Run `pnpm create astro@latest . --template minimal --install --no-git` in the project root
       - Select "Minimal" template
       - Select "Yes" for installing dependencies
       - Select "No" for initializing git (already initialized)
    2. Verify package.json has astro 5.x in dependencies

    IMPORTANT: Do NOT use @astrojs/tailwind (deprecated). Use @tailwindcss/vite instead.
  </action>
  <verify>
    <automated>test -f package.json && grep -q '"astro"' package.json</automated>
  </verify>
  <done>package.json exists with Astro 5.x dependency, pnpm install completed successfully</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add React and Tailwind integrations</name>
  <files>package.json, astro.config.mjs, tsconfig.json</files>
  <action>
    1. Run `pnpm astro add react -y` to add React integration
    2. Run `pnpm add tailwindcss @tailwindcss/vite` to install Tailwind 4.x with Vite plugin
    3. Update astro.config.mjs to include:
       - site: 'https://wangjs-jacky.github.io'
       - base: '/frontend-design-gallery'
       - vite.plugins: [tailwindcss()]
    4. Update tsconfig.json to include jsx: "react-jsx" and jsxImportSource: "react"

    CRITICAL: @astrojs/tailwind is DEPRECATED. Must use @tailwindcss/vite plugin approach.
  </action>
  <verify>
    <automated>grep -q "@tailwindcss/vite" package.json && grep -q "tailwindcss()" astro.config.mjs</automated>
  </verify>
  <done>React and Tailwind installed, astro.config.mjs configured with correct base path for GitHub Pages</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create global CSS with Tailwind import</name>
  <files>src/styles/global.css</files>
  <action>
    1. Create src/styles/ directory if not exists
    2. Create src/styles/global.css with:
       - @import "tailwindcss" at the top
       - CSS custom properties for Terminal Noir theme:
         - --color-bg: #0a0a0b
         - --color-bg-elevated: #131316
         - --color-text: #e5e5e7
         - --color-text-muted: #6b6b76
         - --color-border: rgba(255, 255, 255, 0.06)
         - --color-primary: #00ff88
         - --color-primary-dim: rgba(0, 255, 136, 0.15)
         - --color-amber: #ffb800
         - --color-red: #ff4757
         - --color-blue: #00d4ff
         - --glass-bg: rgba(255, 255, 255, 0.02)
         - --glass-border: rgba(255, 255, 255, 0.05)
  </action>
  <verify>
    <automated>test -f src/styles/global.css && grep -q "@import.*tailwindcss" src/styles/global.css</automated>
  </verify>
  <done>Global CSS file exists with Tailwind import and Terminal Noir CSS variables</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Set up Vitest for testing</name>
  <files>vitest.config.ts, package.json</files>
  <action>
    1. Run `pnpm add -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react`
    2. Create vitest.config.ts with:
       - test.environment: 'jsdom'
       - test.globals: true
       - test.setupFiles: ['./src/test/setup.ts']
       - Include React plugin for component testing
    3. Create src/test/setup.ts with @testing-library/jest-dom import
    4. Add "test": "vitest" and "test:run": "vitest run" scripts to package.json
  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q '"test"' package.json</automated>
  </verify>
  <done>Vitest configured and test commands available in package.json</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Verify build and development server</name>
  <files></files>
  <action>
    1. Run `pnpm build` to verify static build works
    2. Verify dist/ directory is created with index.html
    3. Run `pnpm dev` briefly to ensure dev server starts (then stop it)

    Expected: Build completes without errors, dist/ contains static files.
  </action>
  <verify>
    <automated>test -d dist && test -f dist/index.html</automated>
  </verify>
  <done>pnpm build succeeds, dist/ directory created with static output</done>
</task>

</tasks>

<verification>
- Run `pnpm build` - must complete without errors
- Run `pnpm dev` - must start without errors (manual check)
- Verify Tailwind classes work by adding a test element with Tailwind class
- Verify React component can be created and imported
</verification>

<success_criteria>
- [ ] Astro 5.x project initialized in project root
- [ ] React 19.x integration added via @astrojs/react
- [ ] Tailwind 4.x configured via @tailwindcss/vite (NOT @astrojs/tailwind)
- [ ] astro.config.mjs has site and base for GitHub Pages
- [ ] Global CSS with Tailwind import and Terminal Noir variables exists
- [ ] Vitest configured for testing
- [ ] pnpm build succeeds
- [ ] pnpm dev starts development server
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
